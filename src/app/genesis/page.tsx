import GenesisAnimation from "@/components/ui/GenesisAnimation";

/**
 * /genesis — Full-screen 5-phase Catalysis fight-to-logo animation.
 *
 * Server Component — all client animation logic lives in GenesisAnimation.tsx.
 * Accessible at: http://localhost:3000/genesis
 */
export default function GenesisPage() {
  return <GenesisAnimation />;
}
